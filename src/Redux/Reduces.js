const {NUEVASTARJETAS, AUMENTAR} = require('../Action-Types/Types');

const initialState = {
    contador: 0,
    characters: []
}

function characters(state = initialState, action) {

}

module.exports = characters